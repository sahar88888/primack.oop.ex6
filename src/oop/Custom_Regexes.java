package oop;

/**
 * A library of  custom regexes for our use.
 */
 public class Custom_Regexes {
    public static String VAR_TYPE = "[int|double|String|boolean|char]";
    public static String NAME = "[^\\W\\d]+[\\w]*";// a pattern for a name. (e.g. A1, Zorg_42).
}
